@model HomeViewModel
@{ ViewBag.Title = "Ana sayfa";}
@await Html.PartialAsync("Carousel")
<h2>Haftanın tercih edilen içecekleri</h2>
<h4>Müşterilerin tercihleri - sadece sizin için</h4>

<div class="row">
    @foreach (var drink in Model.PreferredDrinks)
	{
		@await Html.PartialAsync("DrinksSummary",drink)
	}
</div>